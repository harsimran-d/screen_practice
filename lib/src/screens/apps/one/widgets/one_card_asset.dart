import 'package:flutter/material.dart';

class OneCardAsset extends StatelessWidget {
  const OneCardAsset({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/one/welcome_screen_image.png",
    );
  }
}
